NAME:
   enaptercli.test blueprints inspect - Get blueprint metainfo

USAGE:
   enaptercli.test blueprints inspect [command options] <blueprint id or name>

OPTIONS:
   --help, -h  show help

ENVIRONMENT VARIABLES:
   ENAPTER3_API_TOKEN  Enapter API access token
   ENAPTER3_API_HOST   Enapter API base URL (https://api.enapter.com by default)

